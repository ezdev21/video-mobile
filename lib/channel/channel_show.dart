import "package:flutter/material.dart";

class ChannelShow extends StatefulWidget {
  ChannelShow({Key? key}) : super(key: key);

  @override
  _ChannelShowState createState() => _ChannelShowState();
}

class _ChannelShowState extends State<ChannelShow> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}